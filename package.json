{
  "name": "galaxypedia-hugo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "hugo && pnpm pagefind --site public",
    "serve": "pnpm build && hugo server -D"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-go-template": "^0.0.15"
  },
  "dependencies": {
    "pagefind": "^1.2.0"
  }
}
