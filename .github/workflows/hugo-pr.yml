name: Pull Request Validator
on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: build
        uses: .github/workflows/hugo-build.yml@main
        with:
          production: false
      - name: Check for broken external links
        run: pnpm linkrot
